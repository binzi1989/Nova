# NOVA AgentOS P0-P5 产品与工程路线

这套分级不是功能数量，而是 NOVA 能否把真实任务稳定闭环的成熟度标准。任何一级只有通过对应验收门槛，才进入下一阶段。

## P0 · CONNECT｜可连接、可观察、可恢复

目标：桌面端、CLI、网页扩展和微服务使用同一套 AgentOS Runtime。

交付范围：

- Extension Gateway 本机安全边界。
- `nova` CLI：状态、诊断、任务查询、任务请求、事件观察、交付物清单。
- 会话发现、访问令牌、只读权限与外部请求审阅。
- 统一任务 ID、Hook 信封和错误格式。

验收门槛：

- CLI 与桌面端看到同一批任务。
- 未授权请求稳定返回 401，非白名单写请求稳定返回 405。
- 外部请求不能直接调用模型、消耗 Token 或修改文件。
- 中断后上下文和任务状态仍可恢复。

## P1 · UNDERSTAND｜更少 Token，更完整理解

目标：建立 Smart Context Governor 和 Task Capsule。

交付范围：

- L0 任务核心、L1 相关证据、L2 原始资料三层上下文。
- 文件指纹、增量摘要、工具结果去重。
- 多 Agent 共享 Context Pack，只传角色增量。
- 经济、均衡、深度、自定义四档预算。
- 软预算降级，不再因硬上限直接失败。
- `nova context inspect/explain` 与 `nova budget show/estimate`。

验收门槛：

- 同类多轮任务的重复上下文 Token 至少下降 35%。
- 每轮可以解释选择和排除资料的原因。
- 达到软预算后能够压缩、降并发或申请追加，而不是无提示失败。

## P2 · REASON｜目标编译与证据推理

目标：建立 Mission Compiler、Evidence Compass 和 Shadow Run。

交付范围：

- 模糊目标编译为 Mission Charter。
- 自动识别阻塞未知项与非阻塞未知项。
- 任务相关分析矩阵，而不是堆砌固定指标。
- 事实、推断、未知项、反证分层。
- 高成本任务执行前进行路线、预算与权限推演。

验收门槛：

- 每条重要结论可以回到证据或明确标记为推断。
- 缺少资料时不编造，并能给出最高价值补充资料。
- Shadow Run 的成本估计与实际消耗偏差控制在可解释范围。

## P3 · DELIVER｜交付物优先、可审阅、可修订

目标：建立 Delivery Compiler 和 Proof Ledger。

交付范围：

- 执行前确定输出格式、文件名、章节和验收条件。
- 非编程任务默认使用可理解的中文文件名。
- 回答、文件、证据和未完成边界统一进入交付工作台。
- 交付物窗体内预览、反馈、修订、接受和版本比较。
- 技术指标默认折叠，用户先看结果与下一步。

验收门槛：

- 声称完成的任务必须存在真实交付文件或明确的非文件结果契约。
- 反馈进入原任务继续修订，不创建丢失上下文的新任务。
- 每个交付物具备版本、来源、验证和审阅状态。

## P4 · COORDINATE｜自适应多 Agent 与事件驱动

目标：建立 Adaptive Agent Mesh、Agent Chain 和 Event Fabric。

交付范围：

- 根据任务复杂度动态决定 Agent 数量与并行度。
- 重叠角色自动合并，低价值 Agent 自动退出。
- Agent 通过输入输出契约自动串联。
- 文件、Git、邮件、计划任务、Webhook 和上游交付触发工作流。
- 关键决策检查点与可恢复执行。

验收门槛：

- 多 Agent 不重复读取完整上下文。
- 任一子 Agent 失败时可以局部重试，不重跑全部流程。
- 自动触发不能绕过预算、高风险授权和停止条件。

## P5 · ECOSYSTEM｜可信 Agent 生态与受控进化

目标：建立 Agent Passport、A2A、扩展 SDK 与团队治理。

交付范围：

- Agent 身份、版本、适用边界、输入输出、权限和真实案例护照。
- Draft、Runnable、Verified、Production、Deprecated 生命周期。
- 签名扩展、隔离 Micro UI、MCP/Skill/Hook/CLI 组件标准。
- A2A 能力发现和协作，但统一经过 AgentOS 治理。
- Evolution Lab 只生成可停用插件，不修改核心代码。
- 团队权限、审计、成本归属和策略模板。

验收门槛：

- 未签名或超权限扩展无法进入正式任务。
- Agent 升级可以回滚，行为变化可以比较。
- 自动进化有开关、预算、候选审阅、沙箱验证和采用记录。

## 当前开发顺序

1. 完成 P0 CLI 与 Gateway 契约。
2. 在同一 CLI 中先暴露 P1 的预算和上下文诊断能力。
3. 将 P2 Mission Charter 作为所有 Goal 模式的统一入口。
4. 用 P3 Delivery Compiler 收紧所有内置与自建 Agent。
5. P4、P5 只在 P0-P3 的可靠闭环通过真实案例后开放。

## 明确不做

- 不为了展示效果固定创建大量子 Agent。
- 不让外部 Hook 绕过审批直接执行高风险动作。
- 不允许插件直接访问 Electron 主进程或核心密钥。
- 不允许 Evolution Lab 静默修改 NOVA 核心。
- 不用“调用次数”和内部指标冒充真实完成度。
