# NOVA Agent Pack 操作方案

这套方案把 NOVA 保持为一个稳定的 AgentOS，把行业差异封装为可启停的 Agent Pack。跨境电商是首个样板，但同一机制可以用于制造、外贸销售、客服、法务辅助、内容生产、项目管理等场景。

## 一、用户如何使用

1. 在 NOVA 选择工作区并连接模型。
2. 打开“扩展坞 → Agents”。
3. 查看 Agent Pack 的适用范围、专业角色、执行步骤、交付物和权限声明。
4. 启用一个 Agent Pack，点击“使用此 Agent”。
5. NOVA 按 Agent Pack 的 `onboarding` 标准展示所需资料、选择项、资料用途和示例；用户不需要知道专业提示词。
6. 选择希望先得到的结果，NOVA 自动把当前线索组装为第一轮任务；资料不齐时保留未知项并指导下一步收集。
7. 在输入框旁确认当前专业 Agent；不需要行业能力时切回“通用 NOVA”。
8. NOVA 在右侧显示计划、角色分工和执行证据；涉及发布、账号、广告、购买或桌面操作时仍单独确认。
9. 在交付工作台检查文件、数据来源、未知项和 Proof-of-Done，再决定继续追问、返工或归档。

当前版本遵循“一项任务、一个主 Agent Pack”。Pack 内可以定义多个专业角色并由 Agent Mesh 调度；MCP 和 Skills 作为工具能力补充，不会改变主 Pack 的交付责任。这样可以避免多个行业规则互相污染。

## 二、导入第三方或内部 Agent Pack

1. 准备一个包含 `nova.industry.json` 的 Pack 文件夹。
2. 在 Agent 中心点击“导入 Agent Pack”。
3. 选择文件夹并阅读系统确认说明。
4. NOVA 只复制 JSON、Markdown、YAML、TXT 等声明文件，不执行包内代码，也不自动授予权限。
5. 打开“查看结构”，核对角色、流程、交付模板和外部动作声明。
6. 手动启用；先用一个非敏感工作区完成小规模验收，再进入真实业务。

## 三、新行业落地的标准路径

| 阶段 | 需要回答的问题 | 必须留下的产物 | 通过标准 |
|---|---|---|---|
| 1. 业务聚焦 | 谁付费、最耗时的工作是什么 | 行业任务清单 | 先选 1 个高频、可验收任务 |
| 2. 结果定义 | 用户最终拿什么继续工作 | 交付模板 | 不是泛泛回答，而是文件/表格/决策 |
| 3. 角色拆分 | 哪些判断需要不同专业视角 | Agent roster | 每个角色有输入、输出和边界 |
| 4. 流程固化 | 哪些步骤可并行、依赖什么 | Workflow JSON | 每步有 owner、output、acceptance |
| 5. 证据治理 | 哪些是事实、推断、未知项 | Knowledge 与证据规则 | 易变化事实必须带来源和日期 |
| 6. 权限声明 | 会访问哪些外部系统 | Manifest 权限表 | 默认无权限，外部动作逐类确认 |
| 7. 真实测试 | 什么案例能证明有价值 | Evaluation cases | 至少 5 个真实案例可重复验收 |
| 8. 小范围发布 | 谁先试、如何收集反馈 | 验收报告 | 完成率、耗时、返工率可衡量 |

## 四、建议的产品组合

```text
NOVA AgentOS
├─ 通用 Threadspace、任务、权限、预算、恢复、证据
├─ Agent Pack（行业方法与交付责任）
│  ├─ 跨境电商
│  ├─ 制造业询报价
│  ├─ 外贸客户开发
│  └─ 售后与知识运营
├─ MCP（连接平台、数据和服务）
├─ Skills（可复用操作方法）
└─ Model Providers（DeepSeek / OpenAI / Kimi / Ollama / Custom）
```

Agent Pack 回答“这项业务该怎样完成并验收”；MCP 回答“能连接什么”；Skill 回答“某个动作怎样重复执行”；模型负责推理生成；AgentOS 负责边界、调度、落盘和证据。四层不混在一起，后续扩展才不会重写主程序。

## 五、跨境电商 Agent v0.3 的实际能力

装载 `NOVA 跨境商品决策 Agent` 后，模型会额外获得四个只读、确定性领域工具；切回
通用 NOVA 后这些工具不会出现在模型工具清单中：

1. `commerce_normalize_product_passport`：建立商品身份档案，强制分离事实、假设和未知项。
2. `commerce_calculate_landed_profit`：计算关税、进口税、平台费、支付费、达人佣金、广告、退货后的贡献利润与盈亏平衡 ROAS。
3. `commerce_build_evidence_ledger`：检查来源、日期、置信度、过期证据和冲突值。
4. `commerce_assess_market_demand`：从需求、竞争、差异化、本地适配、内容表现与风险等非财务维度形成证据加权判断，并输出下一步验证建议。

推荐操作顺序：选择工作区 → 装载跨境 Agent → 按启动引导补充商品线索、图片、市场和平台 → 中立识别商品 →
研究需求与竞争 → 形成需求适配判断 → 条件允许时补充利润与履约 → 设计最小市场验证。成果区会先显示建议和关键
指标，完整说明及真实落盘文件在下方展开；外部发布、投放、账号访问与购买仍需单独授权。

## 六、行业包质量回归（开发者）

每个行业 Pack 用 3 组任务做内部回归：资料完整、资料有限、存在冲突。记录首次可用结果耗时、人工补充次数、真实文件数、关键事实有来源的比例、返工次数和最终是否可直接用于下一业务动作。这个过程属于开发质量保障，不作为用户界面的独立模块。任何“完成”都必须能找到对应交付物或明确说明为什么只能得到条件性结果。
