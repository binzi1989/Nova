# NOVA 1.0 AgentOS · 范围冻结与交付门槛

状态：**FROZEN**

本文件从 0.9 Preview 11 起成为 Windows 主线的唯一 1.0 范围基准。新的模型、动画、面板、平台和自动化能力，除非直接解除本文件中的硬阻塞，否则不得插入 1.0 主线。

## 1. 1.0 到底交付什么

NOVA 1.0 不是“拥有最多按钮的 AI 客户端”，而是一套能把模糊目标可靠收敛为可验证结果的原生 Windows AgentOS：

`目标 → Mission Charter → 任务图 → 有界执行 → 独立验证 → 修复/重试 → PROVEN 或 BLOCKED`

1.0 的承诺只有五条：

1. **结果真实**：没有真实变更或证据时绝不显示完成。
2. **执行可控**：暂停、取消、预算和权限在真实执行链上生效。
3. **失败可恢复**：崩溃或重启后从已持久化安全点恢复，不重复产生副作用。
4. **边界可信**：工作区、MCP、Skills、命令和桌面动作都不能绕过授权。
5. **状态一致**：会话、任务图、Supervisor、证据账本与交付物对同一任务给出同一结论。

## 2. 当前完成度

以下比例是基于代码路径和交付链审计的工程判断，不是市场宣传分数。

| 维度 | 当前估计 | 已具备 | 距离 1.0 的主要缺口 |
|---|---:|---|---|
| 产品与交互表面 | 87% | 全屏 Threadspace、完成态净化、结果优先交付台、Mission 卡、多轮对话、执行流 | 首次使用、恢复动作、状态真值和无障碍仍需收口 |
| Agent 功能面 | 76% | OpenAI/DeepSeek、工具、MCP、Skills、多 Agent、Goal Mode | 功能已足够，停止横向扩张 |
| 目标到结果闭环 | 88% | Mission、成功信号、Council、工程完整性门槛、定向修复、证据失效、单调执行日志 | 固定端到端任务集仍需真实模型跑满 90 次 |
| 执行可靠性 | 88% | 暂停门、弹性硬预算、副作用收据、原子租约、退出屏障、可重放执行日志 | 仍需长时间真实任务与 UI 终态一致性验收 |
| 安全发布与运维 | 55% | 单实例、崩溃报告、凭据管理器、五边界故障注入、可信发布硬门禁 | 正式证书、真实 HTTPS 更新源和签名安装实测 |
| **综合** | **约 82%** | 内部执行真值与 GA 门禁已经成形 | 剩余工作集中在真实模型基准、Truthful UX 验收和外部发布身份 |

结论：Preview 12、Preview 13 的内部执行主干已经退出；Preview 26 进入 RC1/RC2 验收准备。仍未完成的不能用代码伪造：30 项真实模型基准、六界面 Truthful UX 验收、Windows 发布证书与真实 HTTPS 源。

## 3. 1.0 支持范围

### 正式支持

- Windows 10/11 x64，.NET 8 WPF 原生客户端。
- OpenAI 与 DeepSeek 两个模型提供商。
- Ask、Plan、Build、Goal 四个主模式。
- 单个本地工作区；所有文件和命令操作受工作区边界约束。
- stdio 与 Streamable HTTP MCP；扫描只读，导入、启用、测试和调用分别授权。
- 本地 Skills 注册、检查和有边界执行。
- 2–4 个有界子 Agent 并行分析，以及受控 Worktree Tournament。
- Mission Charter、任务图、证据账本、交付物版本和阶段级恢复。
- 效率总结与本地认知知识图谱。

### Preview / 实验性

- Autopilot、OPERATE/MIXED 目标类型和有限桌面控制。
- 应用打开期间的计划任务。
- Mac 客户端继续使用独立 Preview 路线，不宣称与 Windows 1.0 功能对等。

### 明确延期到 1.x

- 后台 Windows Service、系统托盘和关窗后持续执行。
- 云端任务、跨设备同步、远程多宿主接管。
- 企业 RBAC、团队管理、计费、插件商店。
- 不受限的 Computer Use、自动登录、自动发送、支付或发布。
- 自动安装来源不明的 MCP/Skills。
- 新的模型提供商和无限层级子 Agent。
- Mac 功能追平、Developer ID、公证和 Universal 正式分发。
- 新知识图谱视图、新动画体系和纯视觉重构。

## 4. 必须补齐的 14 个主阻塞项

### P0 · 执行完整性

1. **真实暂停安全点**：每次模型轮次、工具调用、并行批次和验证阶段前都经过统一暂停门。
2. **预算硬限制**：并发 Agent、模型轮次、工具次数、运行时长达到上限后停止调度并进入明确终态；控制中心不再只显示计数。
3. **副作用收据**：写文件、命令、MCP 和桌面动作拥有 Intent/Commit、幂等键、前后指纹和审批引用。
4. **原子租约与 epoch**：同一 Task ID 在任意时刻最多存在一个可执行宿主；单实例互斥不能替代任务级租约。
5. **统一执行日志**：Mission、TaskSnapshot、TaskGraph、Supervisor、Evidence 和 Artifact 以单调序列号协调提交；其他存储是投影，不各自猜测终态。
6. **退出落盘屏障**：正常关窗先停止接收动作，等待当前安全点和关键日志落盘，再标记 clean exit。

### P0 · 结果证明

7. **最多三次修复回路**：PARTIAL 时只针对未满足成功信号重规划、执行、复验；不从头盲跑。
8. **证据失效**：工作区或交付物指纹变化后，旧证据自动降级为 STALE，不能继续支撑 PROVEN。
9. **统一故障分类**：至少区分模型、网络、工具、权限、预算、构建、验证和主机中断，并给出可恢复动作。
10. **确定性验证优先**：能用构建、测试、文件指纹或结构检查证明的信号，不能只依赖模型评审。

### P1 · 产品真值与发布

11. **单一状态真值**：左侧任务、Threadspace、Mission 卡、执行流、AgentOS 中心和交付物使用同一终态映射。
12. **低学习成本恢复**：首次进入 5 分钟内能完成“选工作区—连模型—给目标—授权—看证据”；BLOCKED/PARTIAL 必须直接提供“补充信息、授权、重试未满足项、查看证据”。
13. **安全与故障测试**：覆盖路径越界、提示注入、恶意 MCP/Skill、密钥脱敏、崩溃注入和重复副作用。
14. **可信发布链**：Windows 可执行文件和安装器签名；更新清单使用真实 HTTPS 地址并验证签名，否则 1.0 明确禁用自动更新。

## 5. 固定路线

| 里程碑 | 唯一主题 | 交付内容 | 退出条件 |
|---|---|---|---|
| Preview 12 | Execution Integrity | 暂停门、预算拦截、副作用收据、退出屏障、原子租约 | 暂停后不再发起新动作；重复执行副作用为 0 |
| Preview 13 | Outcome Repair Loop | 三次定向修复、证据失效、故障分类、统一执行日志 | PARTIAL 能恢复；任何 Completed 都有有效证据 |
| RC1 | Truthful UX | 单一状态投影、恢复操作、首启引导、DPI/键盘/性能收口 | 六个主要界面无状态冲突、无伪按钮、无阻断性压盖 |
| RC2 | Security & Release | 安全审计、故障注入、签名安装器、可信更新或明确禁用 | 全部硬门槛通过，可重复构建发布 |
| 1.0 GA | Freeze | 只修阻塞缺陷、迁移说明、发布说明 | 不再引入新能力 |

建议以 **5–7 个聚焦开发循环**完成：Preview 12 和 Preview 13 各允许两个循环，RC1、RC2 各一个循环，最后一个循环只做回归和发布。签名证书、发布域名或真实模型环境若未准备好，只影响 RC2，不应诱发新的产品功能。

### Preview 12 当前进度

- [x] 统一执行安全门与真实暂停。
- [x] 并发 Agent、工具调用、模型轮次和输出字符预算硬限制。
- [x] `BUDGET EXHAUSTED` 独立可恢复终态与控制中心状态。
- [x] 正常退出异步落盘屏障。
- [x] 副作用 Intent/Commit 幂等收据与不确定状态阻断。
- [x] 操作系统独占任务锁、单调 epoch 与双宿主冲突拒绝。

Preview 12 在 2026-07-28 完成代码收口。预算已按完整多 Agent 任务重新校准，
只统计真实模型请求；Ask / Plan / Build / Autopilot / Goal 的任务级模型上限为
24 / 40 / 96 / 180 / 280，达到 80% 时先提示，达到硬上限才在下一安全点停止。

### Preview 13 当前进度

- [x] 编码任务完整工程契约，不再接受单文件演示或模型自述作为交付。
- [x] Git Diff、未跟踪文件内容和非 Git 工作区指纹的真实增量检测。
- [x] 构建/测试、代码审查和工程完整性联合硬门槛。
- [x] 按执行模式预算运行多轮定向工程修复。
- [x] `ENGINEERING NOT READY` 独立状态、完整性成果卡和 JSON 证据。
- [x] Goal Mode 按单个未满足成功信号重规划、执行和复验，最多三轮且通过项证据冻结。
- [x] 工作区或交付物变化后自动把旧 Outcome Evidence 降级为 STALE。
- [x] 模型、网络、工具、权限、预算、构建、验证和主机中断统一故障分类。
- [x] 统一单调执行日志；Task Snapshot、Task Graph、Supervisor 与任务终态共享 `ExecutionSequence`，并可从损坏尾记录前的单调前缀重放。

Preview 13 的工程交付闭环与成功信号级定向修复已在 Preview 19 完成；Preview 26
补齐统一单调执行日志、状态投影序号和重放故障测试，Preview 13 正式退出。

## 6. 1.0 硬性验收门槛

### 正确性

- 任何无真实变更、无验证证据的任务都不能进入 Completed。
- PROVEN 的每个成功信号都能回溯到新鲜证据、产生证据的动作和对应审批。
- 工作区变化后旧证据必然失效。
- PARTIAL、BLOCKED、FAILED、CANCELLED 和 BUDGET_EXHAUSTED 不互相混用。

### 执行与恢复

- 在模型、工具、写入、验证和交付五类边界进行强制终止；恢复后最多丢失一个已声明的持久阶段。
- 对五类边界各重复 20 次故障注入：无越权、无重复写入、无双活动租约。
- 暂停后当前不可中断动作可结束，但不得启动下一模型轮次、工具或并行批次。
- 取消或终态落盘后不得继续产生后台动作。
- 预算达到上限时 100% 阻止下一次受限动作。

### 质量与性能

- 30 个固定端到端任务各运行 3 次；可行夹具任务的 PROVEN 率至少 80%，终态分类正确率至少 90%。
- 现有业务测试全部通过，并新增执行收据、恢复、预算和证据失效测试。
- 500 个历史任务/租约加载不能阻断 UI 首帧；所有长 I/O 在 UI 线程外执行。
- 动画在后台、窗口不可见或系统减少动态效果时停止；动画不能抢占执行流刷新。

### 安全与发布

- 权限绕过、工作区越界、明文密钥落日志和未授权 MCP/Skill 执行为 0。
- 安装包、主程序、版本号、SHA-256 和发布清单一致且可由全新用户环境验证。
- 没有可信更新签名时，产品内不存在可点击的“自动更新”承诺。

## 7. 收紧规则

从现在开始，需求进入主线前必须回答：

1. 它是否直接解除上述 14 个阻塞项之一？
2. 它是否能被自动或可重复地验收？
3. 它是否增加新的权限面、平台面或状态源？

第一问为“否”则进入 1.x；第二问为“否”则先定义验收；第三问为“是”则默认延期。1.0 阶段允许删除、合并和降级功能，不允许用更多 UI 掩盖未完成的执行链。
