# NOVA Agent Creation Standard 1.0

NOVA 用统一契约保证 Agent 可运行、可检查、可校准和可组合，但不统一行业知识、工作方法与判断风格。

## 标准结构

每个新 Agent Pack 应包含：

- `nova.industry.json`：身份、场景、多样性参数、输入输出、安全、校准与协作契约。
- `agent-card.json`：供 Agent Fabric 发现和委派的机器可读能力卡。
- `INDUSTRY_CHARTER.md`：客户、目标、行业边界、事实与未知项规则。
- `agents/AGENT_ROSTER.md`：角色职责和输出边界。
- `workflows/entry-workflow.json`：每一步的负责人、输出和验收条件。
- `delivery-templates/`：最终用户可以直接检查的交付结构。
- `evaluations/`：标准输入、资料缺失、用户纠正、权限拒绝和中断恢复测试。
- `certification.json`：NOVA 自动生成并随 Pack 保存的体检结果。

Agent Pack 只能包含声明、知识和模板，不能携带或执行脚本、二进制文件与安装程序。

## 多样性轴

统一标准不等于同质化。Agent 可以分别声明：

- 场景：研究、决策、内容、工程、操作、数据、服务、监控、合规、协调。
- 自主程度：辅助、审批后执行、目标自治。
- 生命周期：单次、项目、长期、定时。
- 协作方式：独立、专业工作组、主协调 Agent。
- 交付方式：对话、文档、数据、代码、软件操作或混合。
- 判断风格：保守、平衡、探索、创新或合规优先。

这些参数影响默认工作流与验收，但不得放宽 AgentOS 的权限、预算、工作区和 Proof-of-Done 边界。

## 创建与认证

Electron 客户端的 **扩展坞 → Agents → 创建 Agent** 会生成完整 Pack，并执行基础体检：

1. 核心契约文件齐全。
2. 不包含可执行代码。
3. 有首次使用和资料收集引导。
4. 要求真实交付物与 Proof-of-Done。
5. 外部动作保留审批。
6. 包含五类基础评测。

新生成的 Agent 为 `Runnable`，默认停用。通过至少五个真实行业案例并保存回归证据后，才能升级为 `Verified`；真实业务运行、版本治理和监控稳定后，才可标记为 `Production`。

## 用户校准契约

行业 Agent 不通过修改核心源码来迎合用户。用户在交付审查台选择“校准 Agent”后，NOVA 会把纠正保存成独立、可追踪、可回滚的覆盖规则：

- **本轮**：仅影响当前任务，适合临时偏好或例外。
- **当前项目**：仅影响当前工作区，适合品牌、客户或项目约束。
- **该 Agent**：影响这个 Agent 之后的任务，适合行业判断和工作方法修正。
- **组织版本**：作为本机组织版本的长期约定，适合统一格式、证据或语气要求。

每条校准包含版本、类型、来源、作用域和启停状态。原始 Agent Pack 保持不变；运行时按“组织 → Agent → 项目 → 本轮”的顺序叠加，越具体的规则优先。任何校准都不能覆盖 AgentOS 的权限、预算、工作区和 Proof-of-Done 边界。

## 不保证什么

该标准不能保证模型的每个行业判断永远正确。它保证的是：输入输出有边界，过程可以观察，结果可以验证，失败能够暴露，用户可以纠正，更新可以回滚，多个 Agent 可以通过契约协作。
